# ~/.tmuxinator/md_k2.yml

name: K2Competition
root: ~/dev/freelance/meindach/k2-competition
windows:
  - VIM:
      layout: even-vertical
      panes:
        - vim
        - make dev
  - SERVER:
      layout: even-vertical
      panes:
        - sleep 1 && make server
        - sleep 2 && make storybook
  - GIT: git status

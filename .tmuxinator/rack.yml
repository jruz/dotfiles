# ~/.tmuxinator/scw.yml

name: Rack
root: ~/dev/freelance/rack
pre: docker start mariadb

windows:
  - Vim:
      layout: main-vertical
      panes:
        - vim
  - Server:
      layout: main-vertical
      panes:
        - ruby server
  - Git: gs

# ~/.tmuxinator/scw-sinatra.yml

name: SCW Sinatra
root: ~/dev/freelance/scw/sinatra

windows:
  - Vim:
      layout: main-vertical
      panes:
        - vim
  - Server:
      layout: main-horizontal
      panes:
        - dc run -p 4567:4567 --rm app
        - dc up -d nginx
  - Git: gs

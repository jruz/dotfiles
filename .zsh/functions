eslint () {
  if [[ -f ./node_modules/.bin/eslint ]]; then
    ~/dev/frontend/node_modules/.bin/eslint $*
  else
    ~/dev/frontend/node_modules/.bin/eslint $*
  fi
}

chrome_not () {
  google-chrome --force-device-scale-factor=1.8
}
